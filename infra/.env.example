# 说明：此文件是示例。请复制为 infra/.env 并填写真实值，切勿提交真实密钥到仓库。

## 说明
# - 此文件是示例。请复制为 infra/.env 并填写真实值。
# - 切勿提交真实密钥到仓库（已在 .gitignore 中忽略 .env / .env.*）。

# --- MySQL（infra/docker-compose.yml 使用） ---
MYSQL_ROOT_PASSWORD=<REDACTED>
MYSQL_DATABASE=eblog
MYSQL_USER=eblog
MYSQL_PASSWORD=<REDACTED>

# --- MySQL（infra/docker-compose.prod.yml 使用的变量名） ---
DB_NAME=eblog
DB_USER=eblog
DB_PASSWORD=<REDACTED>

# --- MinIO ---
MINIO_ROOT_USER=<REDACTED>
MINIO_ROOT_PASSWORD=<REDACTED>

# --- Backend/Deploy（infra/docker-compose.prod.yml） ---
SERVER_DOMAIN=localhost
SERVER_PORT=80
COOKIE_SECURE=false
SMTP_HOST=<REDACTED>
SMTP_PORT=587
SMTP_USER=<REDACTED>
SMTP_PASSWORD=<REDACTED>

# 可选：对外暴露端口（本地开发可用，生产建议仅内网）
MYSQL_PORT=3306
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
